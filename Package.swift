import PackageDescription

let package = Package(
    name: "HLSCore",
    
    targets: [
        Target(name: "Types",
               dependencies: []),
        Target(name: "Serialization",
               dependencies: []),
    ]
)
